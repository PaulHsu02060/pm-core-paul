# PMW 階段2 新核心 規格

## 0. 文件定位
- 這份是階段2「新核心」的可執行規格。承接 Phase 1（config/seed 抽離,已完成,commit 9f5235b）。
- 來源:接續對話包既定方向 + 2026年於對話中談定的工時負荷引擎與相依衝突細節。

## 1. 整體架構（分層,資料單向往下）
- HTML 操作介面（唯一入口）: 綜合甘特 / 單專案甘特 / 週報 / 月報 / 每日工時回填。
- 統一資料模型（鎖死）。
- 三大引擎: 排程 / 工時負荷 / 報告層。
- 資料家 Google Sheet（純後端,路線甲,單向真相,人不直接編輯）。
- 工作日曆（一級資料表: 國定假日 + 補班 + 公司行事曆,所有日期計算讀它）。
- 通道: HTML ↔ Apps Script ↔ Sheet。

## 2. 統一資料模型（鎖死欄位）
每 task 欄位:
- `wbs`
- `project`
- `phase`（選填）
- `dept`
- `name`
- `taskType`
- `predecessor`（FS / SS / FF / SF + lag）
- `estHours`（算負荷用）
- `owner`
- `parentWbsId`（子綁父）
- `start` / `end`（前端排程算）
- `actualStart` / `actualEnd`
- `progress`
- `status`
- `mustDeliver`
- `deliverable` / `deliverableLink`
- `risk`
- `note`
- `recurrence`
- `creator`

規則:
- phase 選填維度（J 系列完整 WBS,他案跳階段）。
- 父子綁定,工時真相在子 task,父只分類不帶工時。
- 群組用字母編號列。
- 父刪 → 子歸該專案。
- owner 與 creator 分欄。
- cache 分當月可編輯 + 過月唯讀月檔（每月結算一次只讀）。

## 3. 引擎1 排程引擎
- 從後端 `02_formulas.gs` 的 Sheet `WORKDAY()` 改為前端 JS 重寫: FS / SS / FF / SF + lag + WORKDAY（跳週末 + 假日 + 補班,讀工作日曆）,前置即時重算。
- 公式透明層: 計算搬前端後 hover 顯示真實算式。
- 相依衝突處理（今日定案）: 第一版做 **甲 + 乙**。
  - **甲（地基）**: `parsePredecessors` 解析前置 + `isTaskBlocked` 衝突偵測 + 警示（前置未完成 / 已提早,日期可能要調整,紅字提示,不自動改日期）。
  - **乙**: 自動順延「直接後續一層」,不連鎖。對短鏈 A→B→C: A 變 → B 自動移,C 偵測到衝突跳警示。
  - **丙（完整連鎖重排）**: 架構預留,後續啟用。共用地基 `parsePredecessors` / `isTaskBlocked`。

## 4. 引擎2 工時負荷引擎（今日定案,本階段重點）
- 攤算: **模板A 均攤**。`estHours ÷ 跨越工作日 = 每日預設分攤`。模板B（每日上限）架構預留,第一版不啟用。
- 每日分段回填: 任務跨越的每一天一個下拉,0.5h ~ 8h（半小時一格）,填當天實際工時。
- 四個邊界規則:
  - **規則1(完成)**: 累計達總工時 = 完成,不需額外完成勾;按自動排程後,做完多出來的天「整個移除」,任務 bar 縮短（例 6h 原排 3 天,做了 4 + 2 = 6h,週三項目移除,bar 縮成 2 天）。
  - **規則2(溢出)**: 剩餘攤不下 → 最後一天顯示超量紅字預警,不自動順延日期。
  - **規則3(上限)**: 下拉 8h 是單格物理上限;爆量判斷看「各人跨專案當天加總 vs 每日容量」。
  - **規則4(重攤)**: 已填鎖定為事實,重攤只動未填的未來天;不允許累計超過該任務總工時。
- 負荷視圖: 各人每日跨專案工時加總,超過每日容量 → 爆量紅字。
- 註: 每日分段回填需在資料模型加一層「每日工時 log」（每 task 掛 N 天）,屬對鎖死欄位的擴充,需與月結邏輯對齊。

## 5. 引擎3 報告層
- 查詢層 + 模板層分開,新報告 = 加模板不動引擎。
- 週報自動化前提: 雜事也進系統綁時間。

## 6. 兩種甘特
- **綜合甘特**（Dashboard,可多選專案 + 自選時段,按專案 / 時間分組）+ **單專案甘特**（沿用現 WBS 樣貌,按 PLM 階段分組,前置 badge,卡關鎖頭）。
- 都要 Plan & Actual 雙條。第三種分組以後加。
- **坑（時區跨日）**: 甘特今天線 / 工作天倒數 / 負荷本週 / 月報當月,三台電腦 + 同事不同地點裝置日期不準會悄悄算錯 → 釘死單一日期來源。

## 7. 後續擴充（架構預留,第一版不做）
- 模板B 每日上限
- 自動順延丙連鎖
- 實際負荷重攤
- 權限 gateway

## 8. 後續階段
- **階段3**: 舊 Sheet 92 筆遷入新核心,驗證腳本逐欄比對 + 新舊並行 → 退役舊版。`seed.local.js` 當匯入清單。新 Sheet 已建（複本）,舊正式版 Sheet 另一個。
- **階段4**: 唯讀分享（權限 gateway 改寫 03 權限規則）+ 匯出 Excel + 發模板。發模板前需清 git 歷史中 `5ae6077` 的真資料快照。
